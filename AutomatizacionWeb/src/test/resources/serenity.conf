serenity {
  project.name = "Automatización Web"
  take.screenshots = FOR_EACH_ACTION
  logging = VERBOSE
}

webdriver {
  driver = chrome
  timeouts {
    implicitlywait = 30000
    fluentwait = 30000
  }
}

environments {
  default {
    webdriver.base.url = "http://localhost:3000"
  }
}

headless.mode = false